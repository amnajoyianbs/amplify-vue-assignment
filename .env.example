# Frontend Environment Variables (for Vite)
# Copy this file to .env for local development

# API Endpoint (optional - only needed if using REST API)
# VITE_API_ENDPOINT=https://your-api-id.execute-api.region.amazonaws.com/prod

# AWS Region (optional - defaults to amplify_outputs.json)
# VITE_AWS_REGION=us-east-1

# Development Mode
VITE_DEV_MODE=true

# ============================================
# Lambda Function Environment Variables
# (These are set in amplify/functions/*/resource.ts)
# ============================================

# RDS MySQL Configuration (for Lambda functions)
# Uncomment and set these when you have RDS set up
# DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
# DB_NAME=assetdb
# DB_USER=admin
# DB_PASSWORD=your-secure-password

# ============================================
# Notes:
# ============================================
# - Frontend variables must start with VITE_
# - Lambda environment variables are set in resource.ts files
# - Never commit .env file to git (it's in .gitignore)
# - For production, use AWS Secrets Manager for sensitive data
